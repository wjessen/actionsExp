name: Test Changes in a Matrix

on: push

jobs:
  # changes:
  #   runs-on: ubuntu-latest
  #   outputs:
  #     packages: ${{ steps.packages.outputs.changes }}
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v2
  #     - name: Detect Changed Packages
  #       uses: dorny/paths-filter@v2
  #       id: packages
  #       with:
  #         list-files: "shell"
  #         filters: |
  #           apples:
  #             - 'apples/**'
  #           oranges:
  #             - 'oranges/**'
    
  # test_apples:
  #   runs-on: ubuntu-latest
  #   needs: changes
  #   strategy:
  #     matrix:
  #       app: ${{ fromJSON(needs.changes.outputs.packages) }}
  #   steps:
  #     - name: Check Postive Changes
  #       run: echo "${{ matrix.app }} Changed"


  test_env:
    runs-on: ubuntu-latest
    steps:
      - name: Check Environment
        uses: actions/checkout@v2
        with:
          fetch-depth: 2        
      - run: |
<<<<<<< HEAD
          VAR1=$(git rev-parse ${{ github.ref }}^)
=======
          git fetch
          VAR1=$(git rev-parse ${{ github.ref }})
>>>>>>> e3519d14a0a69f6cf1997e2e47257178a43f42f7
          VAR2=${GITHUB_REF}^
          echo "VAR1: ${VAR1}"
          echo "VAR2: ${VAR2}"

